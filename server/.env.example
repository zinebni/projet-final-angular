# ================================
# SMART QUEUE - CONFIGURATION
# ================================
# Copier ce fichier vers .env pour développement local

# Server
PORT=5000
NODE_ENV=development

# MongoDB
# Local: mongodb://localhost:27017/smartqueue
# Docker: Défini automatiquement dans docker-compose.yml
MONGODB_URI=mongodb://localhost:27017/smartqueue

# JWT
JWT_SECRET=your_secret_key_change_this
JWT_EXPIRES_IN=24h

# CORS - URLs autorisées
CLIENT_URL=http://localhost:4200,http://localhost:3000,http://localhost

# ================================
# INSTRUCTIONS:
# 1. Copiez ce fichier en .env
# 2. Modifiez les valeurs selon votre environnement
# 3. Ne committez JAMAIS le fichier .env (il est dans .gitignore)
# ================================

